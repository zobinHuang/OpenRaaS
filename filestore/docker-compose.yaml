# Docker Version
# version: "3.8"

services:
  filestore:
    build:
      context: ./daemon
    image: filestore
    volumes:
      - ./storage:/daemon/storage
    environment:
      - TZ=Asia/Shanghai
    ports:
      - "10950:10950"
    entrypoint: python main.py