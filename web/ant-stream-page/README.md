# DC Platform Stream Page

This is the video stream web source code for clients in DCP.

# Dependencies
We use the following dependencies:

```bash
# react redux
npm install @reduxjs/toolkit react-redux

# pubsub-js
npm install pubsub-js
```